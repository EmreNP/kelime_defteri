import 'package:flutter/material.dart';

class QuizGameScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Çoktan Seçmeli"),
      ),
      body: Container(
        // Boş sayfa içeriği buraya gelecek
      ),
    );
  }
}
